# Which site's content to load — lets one backend serve multiple demo/sites.
VITE_DORA_SITE_ID=demo

# The deployed @dora-cms/server URL (e.g. your Render service URL).
VITE_DORA_API_URL=http://localhost:4000
